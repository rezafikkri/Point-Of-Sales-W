{
    "require": {
        "codeigniter4/framework": "^4.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "scripts": {
        "dev": "php spark serve --port 8000",
        "test": "./vendor/bin/phpunit --testdox --no-logging"
    }
}
